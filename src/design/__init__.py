"""Turning a described object into the parts needed to crochet it.

The split is deliberate: anything uncertain (what the parts of the thing in a
photo are, and roughly what size each one is) stays a *description*, and every
number a crocheter actually works from -- stitches per round, number of rounds,
which stitch -- is computed here from that description, the gauge and the
stated height. Nothing invents stitch counts.
"""
